// target means where actually replace start to end index


///with copywithin array Function


/// and it's make shallow copy means it's change the main array




const array1 = ['a', 'b', 'c', 'd', 'e'];


const newarray=array1.copyWithin(2,4);

console.log(array1);